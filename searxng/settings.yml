use_default_settings: true

server:
  bind_address: "0.0.0.0"
  port: 8080
  secret_key: "pptx-agent-local-dev-change-me"
  limiter: false
  public_instance: false

search:
  formats:
    - html
    - json

ui:
  static_use_hash: true
